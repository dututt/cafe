/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // basePath: '/cafe',
    // images: {
    //     unoptimized: true,
    // },
}

module.exports = nextConfig
